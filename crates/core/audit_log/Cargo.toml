[package]
name = "audit_log"
version = "0.1.0"
edition = "2021"
authors = ["Cipherce Security Team <security@cipherce.io>"]
description = "Tamper-evident, compliance-ready audit log for Secreton."
license = "Apache-2.0"

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
